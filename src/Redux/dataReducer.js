const initState = {
    user: {
        Password: "",
        OrgKey: ""
    },
    recent: [],
    pinned: [],
}
const dataReducer = (state = initState, action) => {
    return state
}

export default dataReducer;